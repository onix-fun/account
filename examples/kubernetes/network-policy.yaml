apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: {name: account-backend-ingress}
spec:
  podSelector: {matchLabels: {app: account-backend}}
  policyTypes: [Ingress]
  ingress:
    - from:
        - namespaceSelector: {}
      ports:
        - {port: 8080}
        - {port: 9097}
